# 《口袋私教》项目交接文档

> 最后更新：2026-07-17 ｜ 当前版本：v3（平台版本号 `acd3442`）｜ 主干分支：master

---

## 一、项目是做什么的、给谁用的

**一句话**：一个装在手机上的 PWA（网页 App）健身私教，像真人私教一样带用户上完一整节课。

**解决什么问题**：零基础健身者不认识器械、不知道动作怎么做、不知道怎么安排计划、坚持不下来。App 接管这四件事：排计划（练一休一循环）、带训练（每个动作六要素教学+组间倒计时+语音朗读）、管饮食（口述/拍照估算热量+补剂提醒）、自适应（根据主观疲劳反馈自动调整以后的强度）。

**目标用户**：
- 首要用户：20 岁男性，181cm/81.5kg，瘦胖子体型，右臂明显强于左臂（所有单侧动作强制左侧先做），引体向上 2-3 个，健身房小白。
- 次要用户（已支持）：通过 Onboarding 问卷适配不同性别、身体数据、训练经验、场地条件（健身房/居家/户外/纯自重）的任何人——为后续分享/商业化铺路。

**使用场景**：下午 6 点下班 → 打开 App 看"今天练什么" → 路上预习动作 → 到健身房点"开始训练" → 跟着 App 走完热身+6 个动作 → 练完总结+补剂提醒。手机竖屏、深色主题、大按钮（汗手友好）、可离线。

---

## 二、技术栈和项目文件结构

### 技术栈
- **前端**：React 19 + TypeScript + Vite 7.2.4 + Tailwind CSS v3.4.19 + framer-motion 12
- **UI 体系**：自研共享组件（src/components/ 根级）+ shadcn/ui 模板（src/components/ui/，未接入新色板，基本未用）
- **PWA**：vite-plugin-pwa（Workbox 预缓存+离线）、manifest、Service Worker、可装桌面
- **持久化**：localStorage（键空间统一 `koudai-coach:*`），无后端
- **语音**：浏览器 speechSynthesis（TTS 朗读）+ webkitSpeechRecognition（语音输入，仅 Chrome/Edge）
- **AI**：DeepSeek API（浏览器端直连，OpenAI 兼容格式）+ 任意 OpenAI 兼容视觉 API（拍照识热量）
- **路由**：react-router，HashRouter（兼容 GitHub Pages 无 SPA 回退）
- **字体**：Oswald/Inter 自托管（public/fonts/，无 Google Fonts 运行时依赖）
- **部署**：GitHub Pages（仓库 `Xuyang52-8/koudai-coach`，gh-pages 分支），base './' 相对路径

### 目录结构（关键文件）

```
app/
├── 交接文档.md                  ← 本文件
├── index.html                   # 入口 HTML（内联 @font-face 自托管字体声明）
├── vite.config.ts               # Vite 配置：base './'、PWA manifest、workbox
├── package.json
├── public/                      # 静态资产（构建时原样拷贝到 dist）
│   ├── app-icon.svg             # App 图标（SVG，黑底薄荷绿哑铃线稿）
│   ├── zone-*.svg               # 6 张器械区域线稿图（有氧/哑铃/杠铃/器械/绳索/自重）
│   ├── illust-rest.svg / illust-shaker.svg  # 休息日/摇摇杯插画
│   ├── icon-sprite.svg          # SVG 图标雪碧图
│   └── fonts/                   # 自托管字体（Oswald 500/600/700 + Inter 400/500/600/700 的 woff2）
├── src/
│   ├── App.tsx                  # 路由表 + OnboardingGate（无档案首启动重定向 /onboarding）
│   ├── main.tsx                 # React 挂载 + --vh 移动端视口修正 + SW 注册
│   ├── index.css                # 设计 token（CSS 变量色板）、工具类（.crt 扫描线/.num 表格数字/.flicker）
│   ├── data/                    # ★ 内容数据层（改内容改这里，不用动代码）
│   │   ├── exercises.json       # 67 个动作，每个含六要素+venues 场地标签+substitutes 替代链
│   │   ├── program.json         # 4 节课（拉/腿+核心/推/全身）× 4 套场地变体 + 休息日方案 + 规则
│   │   ├── nutrition.json       # 静态营养档案兜底 + 50 种食物估算库 + 补剂 + 饮食建议
│   │   └── onboarding.json      # 欢迎语/安全提示/首周建议
│   ├── lib/                     # ★ 核心逻辑层
│   │   ├── types.ts             # 全部 TS 类型（Exercise/Venue/UserProfile/ScheduleConfig/...）
│   │   ├── store.ts             # localStorage 状态层（useStoreKey 体系：cycle/diet/settings/profile/session/exoverride 等）
│   │   ├── utils-workout.ts     # 今日状态判定（三种排期）、场地课程解析、消耗估算
│   │   ├── profile.ts           # Mifflin TDEE 计算、营养目标、bestVenue 场地优先级
│   │   ├── adjust.ts            # RPE 自适应规则（±2.5/±5kg 档位、下限钳制、次数平移）
│   │   ├── ai.ts                # DeepSeek 文本估算热量 + 视觉 API 拍照估算 + Key 连通测试
│   │   ├── ai-exercise.ts       # DeepSeek 生成自建动作六要素草稿
│   │   └── tts.ts               # TTS 封装（zh-CN，遵守用户开关，倒数专用 speakCountdown）
│   ├── components/              # 共享组件
│   │   ├── AppShell.tsx         # 骨架：Header 插槽 + Outlet + 底部 TabBar（/workout 隐藏）
│   │   ├── RestTimerOverlay.tsx # 全屏组间倒计时（30vw 大数字/最后10秒变色+语音+震动）
│   │   ├── Buttons.tsx / Tag.tsx / ProgressBar.tsx / BottomSheet.tsx / SetDot.tsx 等
│   │   ├── feedback.tsx         # useFeedback（震动+打勾+toast）
│   │   ├── Icon.tsx             # 内联 SVG 图标库
│   │   ├── library/             # 动作库专属（详情 Sheet、自建表单、替代动作 Sheet、区域分类）
│   │   ├── workout/             # 训练专属（重量步进解析、热身参数、区域图映射）
│   │   ├── onboarding/          # 问卷选择器（Onboarding 与 Settings 复用）
│   │   ├── rpe/RpeSheet.tsx     # RPE 三键评价弹层
│   │   └── ui/                  # shadcn 模板组件（基本未用，有预存 lint 错误）
│   └── pages/                   # 9 个页面（路由一一对应）
│       ├── Home.tsx             # / 今日：训练日卡/休息日卡、streak、循环进度、场地标签
│       ├── Onboarding.tsx       # /onboarding 7 步问卷（身体/经验/脂肪/饮食/场地/排期）
│       ├── Preview.tsx          # /preview 课前预习（编号手风琴+六要素+听要领+视频直达）
│       ├── Workout.tsx          # /workout 训练进行（核心页：热身→动作卡→计时→RPE）
│       ├── Summary.tsx          # /summary 练后总结（消耗/补剂打卡/循环推进/下课预告）
│       ├── Rest.tsx             # /rest 休息日（游泳/快走/拉伸三选+打卡+身体感觉）
│       ├── Diet.tsx             # /diet 饮食（三通道记录+双进度条+补剂卡+流水）
│       ├── Library.tsx          # /library 动作库（区域分类+场地筛选+搜索+自建）
│       └── Settings.tsx         # /settings 我的（档案/AI配置/语音/自适应记录/数据/关于）
└── dist/                        # 构建产物（勿提交 git；部署用）
```

---

## 三、已完成的功能清单

**训练系统**
- 练一休一 4 课循环（拉/腿+核心/推/全身），绝不按星期排（可选排期后除外）
- 三种排期模式：练一休一 / 练二休一 / 按固定星期（自选周几），随时可改（`utils-workout.ts`）
- 训练进行页三阶段：热身引导（跑步机坡度12速度4×5分钟）→ 动作卡（重量步进器+组次打卡）→ 全屏组间倒计时（60/90秒、暂停/+15秒/跳过、最后10秒变色+语音倒数+震动）
- 每个动作六要素：器械外观描述/健身房位置/口语化步骤/邪修口诀/组次+保守起步重量/常见错误（康复视角）
- 单侧动作强制左侧先做（右臂强于左臂），右侧完成前按钮锁定
- 锁屏/刷新进度不丢（session 持久化），中途退出二次确认
- RPE 自适应：练完每个动作评"太轻松/刚好/太累"，自动调整该动作以后的重量（±2.5/±5kg，下限 50%）或次数（±2，下限 60%），设置页可查看/重置（`adjust.ts`）

**场地系统**
- 四级场地：专业健身房/居家（瑜伽垫）/户外（单杠双杠跑道）/纯自重
- 每节课 4 套场地变体（如：健身房拉日=高位下拉，纯自重拉日=毛巾划船+超人式）
- 每个健身房动作带替代链，训练中可点"换替代动作"（进度保留、可换回）
- 动作库 67 个动作（含自重/居家/户外全套经典），场地筛选 chips

**个性化**
- 7 步 Onboarding 问卷：性别/年龄/身高体重/经验/旧伤/左右力量差/脂肪分布/目标（减脂/塑形/增肌）/饮食习惯/场地/排期
- Mifflin 公式自动算 BMR/TDEE/热量蛋白目标，"我的"页可改、实时重算
- 左右力量差设置驱动单侧规则

**饮食与补剂**
- 三通道记录：文字口述（DeepSeek 估算）/语音输入/拍照（视觉 API），无 Key 降级本地 50 食物库模糊匹配
- 热量+蛋白质双进度条（目标随档案动态化），口语化评语，全程"约"字估算口吻
- 补剂卡：蛋白粉（训练后 30 分钟时效状态机）+ 肌酸（每天 3-5g）打卡

**其他**
- 休息日页：游泳/快走/拉伸主动恢复三选+打卡续 streak+"很疲惫"顺延
- 自建动作：四步表单+六要素质量门（缺一项禁保存）+ DeepSeek「AI 帮我填」草稿
- 动作库详情带 B 站教学视频直达搜索链接
- TTS 全局开关+倒数子开关+试听；连续打卡 streak；数据 JSON 导出；深色 CRT 质感 UI
- PWA：可装桌面、离线可用、字体自托管（成品运行时零国外依赖，免梯子）

---

## 四、没做完的 / 有 bug 的 / 待优化的（按优先级）

**P0 —— 上线阻塞**
1. **GitHub Pages 尚未上传文件**：gh-pages 分支只有 README，需用户手动上传 dist（24 个文件）并在 Settings→Pages 启用；或用户提供 GitHub PAT（classic，repo scope）后可自动推送。涉及：运维，无代码文件。
2. **iOS 桌面图标缺失**：PNG 图标已删除（MCP 无法传二进制），manifest 用 SVG，iOS Safari 装桌面会回退成截图。解决：有二进制上传通道（git push + PAT）后恢复 `public/app-icon.png`/`app-icon-192.png`，并改回 `vite.config.ts` manifest icons 与 `index.html` apple-touch-icon。

**P1 —— 重要**
3. **后端与商业化未做**（第二批）：密钥登录系统（主密钥→子密钥→激活校验）、防破解（核心数据服务端化+混淆）、APK 打包（PWABuilder/Capacitor）。目前纯前端无后端，密钥无从谈起。涉及：整个 `api/`、`db/` 不存在；可用 backend-building-swarm（tRPC+Drizzle+Hono+MySQL）。
4. **AI 真实联调未做**：DeepSeek/视觉 API 均只经 stub 测试（环境无 Key）；TTS/震动在真机未验证。涉及：`src/lib/ai.ts`、`src/lib/ai-exercise.ts`、`src/lib/tts.ts`。
5. **ESLint 预存 11 个错误**：`src/components/feedback.tsx`（3 个）+ `src/components/ui/*`（8 个 shadcn 模板文件），非功能问题但 CI 会红。

**P2 —— 体验优化**
6. **数据只有导出没有导入**：换域名/换设备后历史无法恢复。涉及：`src/pages/Settings.tsx`（数据卡加导入按钮+JSON 解析回写 store）。
7. **蛋白粉 30 分钟紧迫度不实时刷新**：仅渲染时计算。涉及：`src/pages/Summary.tsx`。
8. **语音输入仅 Chrome/Edge 可用**：其他浏览器置灰降级（预期行为，但可考虑 Whisper API 替代）。涉及：`src/pages/Diet.tsx`。
9. **AI 补全后手改步骤，voiceScript 不联动更新**（仍读 AI 原版）。涉及：`src/components/library/CustomExerciseForm.tsx`。
10. **训练中途杀 App 会重做当前组**（安全方向设计，不跳组，但可优化为组内断点）。涉及：`src/pages/Workout.tsx`。
11. **RPE 首次评"刚好"会产生零调整记录**：Settings 列表显示但不亮"已为你调整"Tag（hasAdjustment 判定），设计如此，若嫌冗余可过滤。涉及：`src/pages/Settings.tsx`、`src/lib/adjust.ts`。

**P3 —— 锦上添花**
12. GitHub Pages 国内访问偶尔慢：待国内镜像（Gitee Pages/云托管，需账号）。运维项。
13. 打字机评语切换瞬间有 30ms 旧文案帧。涉及：`src/pages/Diet.tsx`（useTypewriter）。
14. 自重动作无重量步进器（显示"自重"，设计如此）。涉及：`src/pages/Workout.tsx`、`src/components/workout/weight.ts`。
15. manifest 图标为 SVG，部分老浏览器安装图标可能空白（同 P0-2 一并解决）。

---

## 五、运行和预览的方法

**环境**：Node.js 20+

```bash
# 本地开发
npm install
npm run dev          # 开发服务器（默认 http://localhost:5173）

# 生产构建 + 本地预览
npm run build        # 产物到 dist/（含 PWA SW）
npm run preview      # 预览生产构建
```

**项目仓库（沙箱内）**：`/mnt/agents/output/app`（master 为唯一主线；协作分支均从此切出）。子代理工作树用 `bash /app/.agents/skills/swarm-workspace/scripts/setup-local.sh <分支> $HOME/app-<分支>` 创建。

**线上地址**（待 Pages 启用后生效）：`https://xuyang52-8.github.io/koudai-coach/`
部署方式：把 `dist/` 内全部文件上传到仓库 `Xuyang52-8/koudai-coach` 的 `gh-pages` 分支根目录，Settings→Pages→Deploy from a branch→gh-pages。

**平台历史版本**：v1 `0794375` ｜ v2 `e4a682f` ｜ v3 `acd3442`（当前）

---

## 六、两个 API 的接入位置

> ⚠️ 现状：**两个 API 的 Key 都不是环境变量**，是用户在 App「我的 → AI 配置」页填写后存 localStorage（键 `koudai-coach:settings`）。这样做是因为纯前端无后端，Key 只存在用户自己手机里。

### ① DeepSeek API（文本：口述记饮食估算 + 自建动作 AI 补全）

| 项 | 位置 |
|---|---|
| 调用代码 | `src/lib/ai.ts` → `estimateFoodText()`；`src/lib/ai-exercise.ts` → `generateExerciseDraft()` |
| 请求地址 | `https://api.deepseek.com/chat/completions`（OpenAI 兼容，model 写死 `deepseek-chat`） |
| Key 存储字段 | localStorage `koudai-coach:settings` → **`deepseekKey`** |
| 配置 UI | `src/pages/Settings.tsx`「AI 配置」卡（含 testDeepSeekKey 连通测试） |
| 降级行为 | 无 Key 时饮食估算自动降级本地食物库（`src/data/nutrition.json`），AI 补全按钮置灰 |

### ② 视觉 API（拍照识热量，OpenAI 兼容视觉端点，如智谱 GLM-4V / 通义 Qwen-VL）

| 项 | 位置 |
|---|---|
| 调用代码 | `src/lib/ai.ts` → `estimateFoodPhoto()` |
| 请求地址 | 用户配置的 `{visionEndpoint}/chat/completions`（OpenAI vision 格式，base64 图片） |
| 存储字段 | localStorage `koudai-coach:settings` → **`visionEndpoint`**（如 `https://open.bigmodel.cn/api/paas/v4`）、**`visionKey`**、**`visionModel`**（如 `glm-4v-flash`） |
| 配置 UI | `src/pages/Settings.tsx`「AI 配置」卡「视觉端点三件套」 |
| 降级行为 | 未配置时拍照按钮弹引导 BottomSheet 提示去设置 |

### 如果要改成环境变量（未来建议）

Vite 只暴露 `VITE_` 前缀变量，建议命名：

```
VITE_DEEPSEEK_API_KEY=sk-xxx          # 对应现在 settings.deepseekKey
VITE_VISION_ENDPOINT=https://...      # 对应 settings.visionEndpoint
VITE_VISION_API_KEY=xxx               # 对应 settings.visionKey
VITE_VISION_MODEL=glm-4v-flash        # 对应 settings.visionModel
```

改造点：`src/lib/store.ts` 的 `useSettings` 默认值从 `import.meta.env` 读取作为 fallback，UI 填入值优先。注意：**环境变量打进前端包后等于公开**，真做商业化密钥体系时应把 AI 调用挪到后端（见 P1-3），前端不再持 Key。
